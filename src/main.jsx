import React from "react";
import ReactDOM from "react-dom/client";
import FoundrMatch from "./FoundrMatch.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FoundrMatch />
  </React.StrictMode>
);
